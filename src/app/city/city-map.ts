import { City } from './city';

/**
 * A mapping from regions to their corresponding `City` objects.
 */
export const RegionToCity: Map<region, City> = new Map<region, City>();

/**
 * A mapping from units to their corresponding `City` objects.
 */
export const UnitToCity: Map<unit, City> = new Map<unit, City>();
