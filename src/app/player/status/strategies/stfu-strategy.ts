import { ActivePlayer } from '../../types/active-player';
import { StatusStrategy } from './status-strategy';

export class STFUStrategy implements StatusStrategy {
	run(gamePlayer: ActivePlayer): void {
		// TODO: Implement PlayingStrategy
	}
}
